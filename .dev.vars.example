# Copy to .dev.vars (gitignored) and paste in real keys for `npm run dev:api`.
# In production these two live in the Pages dashboard as encrypted Secrets —
# never in wrangler.toml, which is committed.
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=...
